# Embed Guide (TR/EN)

## 🇹🇷 Türkçe (TR)
"No AAB SEO Audit" aracını web sitenize yerleştirmek için aşağıdaki iFrame kodunu kullanabilirsiniz.

### iFrame İle Yerleştirme
En basit ve hafif yöntemdir. Sitenizin istediğiniz yerine aşağıdaki kodu yapıştırın:

```html
<iframe 
  src="https://tool-domain.com/widget.html?lang=tr" 
  width="100%" 
  height="250" 
  style="border:none; border-radius:12px; overflow:hidden;"
  title="WebFine SEO Audit Tool">
</iframe>
```

**Özellikler:**
- `lang=tr` veya `lang=en` parametresi ile dili ayarlayabilirsiniz.
- Widget içindeki "Analiz Et" butonu, kullanıcıyı otomatik olarak ana araç sayfanıza yönlendirir.

---

## 🇺🇸 English (EN)
To embed the "No AAB SEO Audit" tool into your website, use the following iFrame code.

### iFrame Embedding
This is the simplest and most lightweight method. Paste the following code anywhere on your site:

```html
<iframe 
  src="https://tool-domain.com/widget.html?lang=en" 
  width="100%" 
  height="250" 
  style="border:none; border-radius:12px; overflow:hidden;"
  title="WebFine SEO Audit Tool">
</iframe>
```

**Features:**
- Set the language using the `lang=en` or `lang=tr` parameter.
- The "Audit Now" button inside the widget automatically redirects the user to your main tool page.
